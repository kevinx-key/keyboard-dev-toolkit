# 项目路书 — Keyboard Dev Toolkit（公开仓·社区版）

> **给所有接手模型的第一份文档。** 先读本文件，再按需深入对应章节引用的源码。
> 本路书基于 **2026-08-26 实际代码** 编写（git 分支 `main`，版本 v1.0.29），
> 若仓库有较大演进请同步更新本文件。
> ⚠ 本仓库为**公开仓（社区版）**，Pro 模块为 stub；与私有仓 `keyboard-editor-pro` 的关系见 §1/§6F。

---

## 0. 30 秒快速上手

| 事项 | 说明 |
|------|------|
| 这是什么 | 键盘配列编辑器（KLE 兼容）+ 键盘 PCB/定位板自动生成 + AI 设计助手 |
| 技术栈 | Next.js 16.2.1 / React 19 / TypeScript 5 / Tailwind v4 / Tauri v2 (Rust) |
| 启动 | `npm install`（postinstall 自动注入 stub）→ `npm run dev` → http://localhost:3000 |
| 全量检查 | `npm run check`（lint + typecheck + build） |
| 单测 | `npx vitest run tests/`（12 文件，443+ 用例） |
| E2E | `npx playwright test`（e2e/ 下 3 spec） |
| 单页应用 | 只有一个页面 `src/app/page.tsx` → `EditorPage`（全部功能都在这一页） |
| 入口文件 | `src/components/EditorPage.tsx`（406 行主编排器） |
| AI CLI | `npm run kdt`（`tools/cli.ts`，配合 MCP server 供 opencode/Claude Code 调用） |

---

## 1. 项目背景

- **起源**：逆向克隆 [keyboard-layout-editor.com](https://www.keyboard-layout-editor.com)（原站 AngularJS 1.x + Bootstrap 3），用现代 React/Next.js 独立重写，**保持 KLE 数据格式完全兼容**（JSON `[[...]]` 数组格式 + URLON `##@@` 紧凑格式）。
- **进化**：从"KLE 克隆"演进为**键盘 PCB 设计工具**——配列编辑 + 定位板（Plate）生成 + PCB 板框/开孔生成 + DXF/STP/KiCad/立创EDA/QMK 导出；v1.0.29 起新增 **AI 设计助手**（自然语言描述配列，AI 生成布局）与 **MCP/CLI 工具层**。
- **双仓架构（重要）**：
  - **公开仓（本目录）**：社区版，AGPL-3.0，GitHub `709208969/keyboard-dev-toolkit`。Pro 模块以 stub 占位，clone 后零配置可构建、可运行。
  - **私有仓**：`gitee.com/kevinxu93/keyboard-editor-pro`（master），存放 Pro 模块真实源码（QMK 导出 / KiCad / 立创EDA / keyboard-lceda-helper / 对应测试），由 `scripts/fetch-pro.mjs` 注入覆盖（详见 §8）。
- **目标用户**：机械键盘客制化玩家/团长/小工作室，K星团队自研工具。
- **商业模式**：社区版免费开源（AGPL-3.0，可自部署/自构建）引流，Pro 模块源码私有（`npm run pro:fetch` 注入后构建出 Pro 版）。
- **仓库**：GitHub `709208969/keyboard-dev-toolkit`（origin，SSH host `github_kevinxu`）。本机路径 `K:\0AMAC\kle-editor\keyboard-editor-public`。

### 版本演进速览（git log 摘要，公开仓 19 commits）

```
community edition  — 社区版建仓 + Windows/macOS/Linux 发布管线（GitHub Actions）
v1.0.26           — 桌面版发布（win/mac/linux 安装包）+ README 英文重写
~v1.0.27          — 生产级标准 STEP 3D 模型（含 PCB 卫星轴 STP 下载按钮）
~v1.0.28          — 一键自动更新（Tauri updater + GitHub Release minisign 签名）
v1.0.29           — AI 对话面板（ToolBelt 第 11 Tab）+ tools/ MCP 工具层 + CLI +
                    Karpathy 风格系统提示词；CI 修复（OCCT 预编译缓存自愈/cadrum 构建）
```

> 注：公开仓历史为社区版建仓后的精简历史（旧私有仓历史已重写，勿引用旧 commit hash）。

---

## 2. 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| 框架 | Next.js 16.2.1（静态导出 `out/`） | 全客户端渲染（`page.tsx` 用 `dynamic(..., {ssr:false})`） |
| UI | React 19 + Tailwind CSS v4 | 组件大量使用全局 CSS 类（`.kle-*` 骨架类）+ `--theme-*` 变量 |
| 图标 | lucide-react | 全站图标（旧 emoji 已全部替换） |
| 桌面壳 | Tauri v2（Rust） | `saveFile` / STP 生成 / updater 自动更新 |
| CAD | cadrum (OpenCASCADE) | Rust 侧 DXF→STP 3D 建模 |
| 几何 | polygon-clipping | PCB/Plate 布尔运算 |
| 矩阵引擎 | `lib/matrix-core.ts`（自研） | 配列→矩阵分配（孤儿键检测），PCB 预览/导出共用 |
| AI 工具层 | @modelcontextprotocol/sdk + zod + tsx | `tools/` 无头核心层 + MCP stdio server + CLI |
| 测试 | Vitest（单测）+ Playwright（E2E） | 443+ 单测 + 3 spec E2E |
| 构建 | Next.js 静态导出 + javascript-obfuscator 混淆 | `scripts/obfuscate-build.mjs` |
| 许可 | AGPL-3.0-only | README 注明可重许可（keep dual-licensing possible） |

---

## 3. 目录结构地图

```
keyboard-editor-public/
├── src/
│   ├── app/                    # Next.js 路由（layout.tsx / page.tsx / globals.css 主题令牌）
│   ├── components/             # 组件层（见 §6 功能清单）
│   │   ├── canvas/             #   KeyboardCanvas 子组件（KeyRenderer/LabelRenderer/ContextMenu/CanvasInteraction）
│   │   ├── toolbelt/           #   工具带 11 个 Tab 面板（含 AiTab 523 行）
│   │   └── ui/                 #   通用 UI 组件
│   ├── hooks/                  # useKeyboardEditor / usePlateEditor / usePCBEditor /
│   │                           #   useProjectPersistence / useStpExport / useQmkExport
│   ├── lib/                    # 核心逻辑（解析/序列化/reducer/导出/坐标/i18n/主题/matrix-core）
│   │                           #   ⚠ kicad-export.ts / lceda-export.ts 为 Pro stub
│   ├── plugins/                # 插件系统（index.ts + qmk-export/ ⚠ Pro stub）
│   ├── data/                   # 静态数据（presets / color-swatches / textures）
│   └── types/                  # 空（.gitkeep）
├── pro-stub/src/               # ⚠ Pro 模块占位文件（lib/kicad-export.ts、lceda-export.ts、
│                               #   plugins/qmk-export/{index,types}.ts）——fetch-pro --stub 的注入源
├── plugins/                    # ⚠ keyboard-lceda-helper 注入目标（.gitignore，公开仓不提交）
├── tools/                      # KDT AI 工具层（core.ts 无头核心 / cli.ts / mcp-server.ts / AI_GUIDE.md）
├── tests/                      # Vitest 单测（12 文件；Pro 测试由 fetch-pro 注入）
├── e2e/                        # Playwright E2E（basic / toolbar / theme）
├── scripts/                    # fetch-pro / sync-version / obfuscate-build / build-qmk-pro /
│                               #   generate-latest-json / merge-translations / rebuild-i18n 等
├── .github/workflows/          # ci.yml（检查）+ release.yml（发版，OCCT 预编译缓存自愈）
├── src-tauri/                  # Tauri 桌面壳
│   ├── src/lib.rs              #   Rust 后端（STP 生成 + 模型装配）
│   └── models/                 #   7 个 3D STEP 模型（hotswap/hotswap-pcb/Type-C/4P/MCU/RGB/T4）
├── docs/screenshots/           # README 功能截图
├── public/fonts/               # 自托管字体
├── README.md                   # 英文简介 + 分步本地运行指南（文末含中文简介）
├── .mcp.json                   # kdt MCP server 配置（根目录，已存在）
└── package.json                # 版本号 v1.0.29（tauri.conf.json 同步）
```

---

## 4. 核心数据模型（`src/lib/kle-types.ts`）

一切功能的起点。**内部统一用 `KeyProps[]`，外部兼容 KLE 两种格式。**

### 三种数据格式

| 格式 | 形态 | 用途 |
|------|------|------|
| KLE JSON | `[[{...},{...}], [...]...]` 每行一个数组 | layouts.json 导入、预设、保存文件 |
| URLON | `##@@` 紧凑 URI 编码 | Permalink（URL hash）、URL 分享 |
| KeyProps[] | 内部归一化格式 | 编辑器内部唯一状态 |

### KeyProps — 单键属性（`kle-types.ts:97`）

12 个标签位（`LABEL_POSITIONS=12`，`kle-types.ts:9`）：

```
0=左上 1=上中 2=右上  3=中左 4=中中 5=中右  6=下左 7=下中 8=下右  9=前中 10=前左 11=前右
```

关键字段：`labels[12]`、`align`（对齐模式 0-7，`LABEL_ALIGN_MAP` `kle-types.ts:12`）、
`x/y/w/h`（位置尺寸）、`x2/y2/w2/h2`（异形键第二段）、`r/rx/ry`（旋转）、
`c/t`（底色/字色）、`textColor[]/textSize[]/f2/fa`（逐标签颜色字号）、
`d`（decal 装饰键）、`g`（ghosted 幽灵键）、`l`（stepped 阶梯键）、`n`（homing）、
`sm/sb/st`（switch mount/brand/type 三级级联）、`stab`（卫星轴）。

### 其他核心类型

| 类型 | 位置 | 说明 |
|------|------|------|
| `KLEMeta` | `kle-types.ts:188` | 键盘元数据（name/backcolor/background/css/switch 三级等） |
| `KLELayout` | `kle-types.ts:265` | `{meta, keys, _sourceCache?}` 完整布局 |
| `EditorAction` | `kle-types.ts:275` | 全部 17 种 reducer action 类型 |
| `EditorState` | `kle-types.ts:299` | layout + 选择集 + 剪贴板 + Undo/Redo 栈 |
| `parseLabelColor` | `kle-types.ts:70` | 标签 `#rrggbb:文本` 颜色前缀解析 |
| `getLabelFontSize` | `kle-types.ts:219` | 字号优先级：textSize → fa → f2 → 默认 |
| `MatrixResult` | `lib/matrix-types.ts` | 矩阵分配结果（行列/孤儿键） |

---

## 5. 核心数据流（全链路）

```
URL hash (##@@) / localStorage / 预设 / JSON 文件导入
        │
        ▼
parseKLE() kle-parser.ts:23  ─┐
parseKLEJSON() kle-serial.ts:156 ─┼─► KeyProps[] + KLEMeta
parseLayoutJSON() kle-serial.ts:20 ─┘
        │
        ▼
editorReducer() kle-reducer.ts:22   ← useKeyboardEditor (useReducer)
（Undo/Redo 栈 / 选择集 / 剪贴板 / 17 种 action / _sourceCache 管理）
        │
        ├──► KeyboardCanvas（画布渲染，canvas/ 子组件）
        ├──► ToolBelt（11 Tab 属性编辑，含 AiTab）
        ├──► PlateSection / PCBSection（CAD 生成 + 预览，矩阵走 matrix-core）
        ├──► 导出：JSON / SVG / PNG / JPG / DXF / STP(Tauri)
        │        + Pro 注入后：KiCad / 立创EDA / QMK
        └──► AI 链路：AiTab ↔ tools/core.ts ↔（kdt CLI / MCP server）→ 布局文件
        │
        ▼
自动保存：serializeKLE → URL hash + localStorage（500ms 防抖，LAYOUT_VERSION=3 版本校验）
```

状态入口：`useKeyboardEditor`（`src/hooks/useKeyboardEditor.ts`）——
加载优先级：URL hash → localStorage（版本匹配才读，不匹配清空）→ 默认 ANSI 104 预设。

---

## 6. 功能模块清单（功能定义 + 文件位置 + 代码位置）

### A. 布局编辑核心（数据层）

| 功能 | 定义 | 文件 / 代码位置 |
|------|------|----------------|
| URLON 解析/序列化 | `##@@` 格式 ↔ KeyProps[]，f2/fa 独立 if（勿改） | `src/lib/kle-parser.ts` `parseKLE:23` `serializeKLE:52` |
| URLON 编解码原语 | 紧凑编码，`stringify(NaN)` 必须输出 `:null` | `src/lib/kle-urlon.ts` `stringify:38` `parse:82` |
| JSON 格式解析/导出 | layouts.json ↔ KeyProps[]，坐标追踪（rx/ry 簇） | `src/lib/kle-serial.ts` `parseLayoutJSON:20` `parseKLEJSON:156` `serializeKLEJSON:239` |
| **_rawRows 无损往返** | ⛔ 导入后保留原始行数据，导出完全一致 | `src/lib/kle-serial.ts` `getRawRows:270`（**禁改**，见 §9） |
| 状态管理 | 17 种 action + Undo/Redo 快照栈 + 剪贴板 | `src/lib/kle-reducer.ts` `editorReducer:22` `createInitialState:11` |
| 坐标系统 | **唯一坐标转换入口**（勿在其他文件私建常量） | `src/lib/coordinate-system.ts` `U_MM=19.05:17` `KEY_UNIT_PX=54:18` `kuToPx:56` |
| 几何工具 | rotatePoint / rotatedBbox（旋转包围盒） | `src/lib/geometry-utils.ts` |
| L 形键路径 | ISO Enter / Big-Ass Enter SVG path 计算 | `src/lib/lshape-path.ts` |
| 颜色工具 | lighten/darken（键帽顶面提亮 20%） | `src/lib/color-utils.ts` |
| 卫星轴偏移 | 各尺寸卫星轴位置表 | `src/lib/stab-offsets.ts` |
| 统一 SVG 渲染器 | 键体/标签/异形键 SVG 计算（导出与画布共用） | `src/lib/key-renderer.ts` |
| **矩阵引擎** | 配列→矩阵分配（孤儿键检测/压缩/格式化），PCB 预览与导出共用 | `src/lib/matrix-core.ts` `assignMatrix:34` `compressMatrix:321` `formatMatrix:384` + `matrix-types.ts` |
| XSS 消毒 | 所有用户输入统一消毒层 | `src/lib/sanitize.ts` |
| 错误日志 | installGlobalErrorHandler + 日志下载 | `src/lib/error-logger.ts` |
| Barrel 导出 | lib 统一出口（kle-types/parser/reducer/serial） | `src/lib/index.ts` |

### B. 编辑器 UI（组件层）

| 功能 | 定义 | 文件 |
|------|------|------|
| **主编排器** | 单页编排所有模块（画布+工具带+Plate+PCB+页脚操作+Overlay） | `src/components/EditorPage.tsx`（406 行，`src/app/page.tsx` 动态加载） |
| 顶栏 TopBar | 预设/选项/语言/主题下拉、步骤值设置、帮助 | `src/components/TopBar.tsx` |
| 浮动工具栏 | Add/Delete/Undo/Redo/Cut/Copy/Paste/导出按钮 + 特殊键（ISO/阶梯等） | `src/components/FloatingToolbar.tsx` |
| 键盘画布 | 渲染 + 框选 + 拖拽移动 + Ctrl+缩放 + Alt+平移 + 旋转命中测试 | `src/components/KeyboardCanvas.tsx` |
| └ 键体渲染 | 双层 div（底色+顶面渐变）/ SVG 异形键 | `src/components/canvas/KeyRenderer.tsx` |
| └ 标签渲染 | 12 位置标签 + 颜色前缀 | `src/components/canvas/LabelRenderer.tsx` |
| └ 右键菜单 | 删除/复制/剪切/粘贴/添加 | `src/components/canvas/ContextMenu.tsx` |
| └ 交互工具 | hitTestKey / getKeysInArea（反向旋转命中） | `src/components/canvas/CanvasInteraction.ts` |
| **工具带 ToolBelt** | 11 Tab 属性面板 | `src/components/ToolBelt.tsx`（106 行容器）+ `src/components/toolbelt/` |

### ToolBelt 11 个 Tab（`src/components/toolbelt/`）

| Tab | 功能定义 | 文件 |
|-----|---------|------|
| 键属性 | w/h/x/y/旋转等键尺寸位置编辑 | `PropertiesTab.tsx` |
| 标签 | 12 标签位文本/字号/颜色编辑 | `LabelsTab.tsx` |
| 配色 | 键底色/字色 + 色板 | `ColorsTab.tsx` |
| 键盘 | 键盘元数据（名称/底色/背景/圆角/switch 级联） | `KeyboardTab.tsx` |
| 工具 | Align/Move Legends 图形化、Remove Legends 正则分类、Auto Layout | `ToolsTab.tsx` |
| 原始数据 | URLON/JSON 实时编辑 + 恢复备份 | `RawDataTab.tsx` |
| SVG | SVG 代码预览/复制 | `SvgTab.tsx` |
| 字符 | 特殊字符插入（→ 当前选中键 center-center） | `CharsTab.tsx` |
| 样式 | 键盘级自定义 CSS | `CssTab.tsx` |
| 摘要 | 配列统计（键数/尺寸/switch 分布） | `SummaryTab.tsx` |
| **AI** | AI 对话面板：OpenAI 兼容 API（DeepSeek/通义/OpenAI）、聊天记录 localStorage 持久化、工具操作可折叠查看 | `AiTab.tsx`（523 行，v1.0.29 新增） |

### C. Plate（定位板）与 PCB 生成

| 功能 | 定义 | 文件 / 代码位置 |
|------|------|----------------|
| Plate 生成 | 按配列生成定位板几何（开关孔/卫星轴孔/螺丝孔） | `src/lib/plate-export.ts` `generatePlate:331` |
| PCB 生成 | 板框/开关焊盘/卫星轴焊盘/Type-C/4P/MCU 组件区 | `src/lib/pcb-export.ts` `generatePCB:364` `PCBConfig:25` |
| KiCad 导出 | ⚠ **Pro stub**：真实实现在私有仓，注入后可用 | `src/lib/kicad-export.ts`（公开仓为 throw 占位） |
| 立创EDA 导出 | ⚠ **Pro stub**：同上 | `src/lib/lceda-export.ts`（公开仓为 throw 占位） |
| STP 接口 | 调用 Tauri Rust 生成 3D STEP | `src/lib/stp-export.ts` |
| Plate 预览区 | 交互式旋转覆盖 + DXF/STP 导出 UI | `src/components/PlateSection.tsx` + `InteractivePlatePreview.tsx` |
| PCB 预览区 | 交互式旋转 + 导出 UI（矩阵分配来自 matrix-core） | `src/components/PCBSection.tsx` + `InteractivePCBPreview.tsx` |
| STP 进度遮罩 | 全屏进度条 | `src/components/StpExportOverlay.tsx` |

### D. 导出系统（图片/文件）

| 功能 | 定义 | 文件 / 代码位置 |
|------|------|----------------|
| JSON 导出 | ⛔ 保持 _rawRows 无损 | `src/lib/kle-export.ts` `exportJSON` |
| SVG 导出 | ⛔ 必须同时保留 width/height + max-width:100% | `src/lib/kle-export.ts` `exportSVG` |
| PNG/JPG | SVG → Canvas 渲染 → Blob | `src/lib/kle-export.ts` `downloadPNG` `renderSVGToBlob` |
| 文件保存 | Tauri 环境走系统对话框，Web 走浏览器下载 | `src/lib/platform-bridge.ts` `saveFile` |

### E. 持久化与备份

| 功能 | 定义 | 文件 |
|------|------|------|
| 自动保存 | URL hash + localStorage（500ms 防抖，版本 v3） | `src/hooks/useKeyboardEditor.ts` |
| 项目级保存 | Save/Upload 全部数据（layout+plate+pcb 配置） | `src/hooks/useProjectPersistence.ts` |
| 项目备份列表 | 备份对话框 + 恢复 | `src/components/ProjectBackupDialog.tsx` + `src/lib/project-backup-manager.ts` |
| 布局备份 | JSON 字符串备份 + 恢复 | `src/components/BackupDialog.tsx` + `src/lib/backup-manager.ts` |
| 序列化 | 项目完整数据格式 | `src/lib/project-serial.ts` |

### F. QMK 固件导出（⚠ Pro 模块，公开仓为 stub）

| 功能 | 定义 | 文件 |
|------|------|------|
| 主出入口 | `exportQmkFirmware()` 全流程编排 | `src/plugins/qmk-export/index.ts`（公开仓为 stub） |
| 类型定义 | 全部 QMK 类型（契约文件，--sync-stub 回写） | `src/plugins/qmk-export/types.ts` |
| 完整实现 | 矩阵分配/keycode 映射/keyboard.json/keymap.c/VIA/引脚分配/配列识别 等 14 文件 | 私有仓 `keyboard-editor-pro` 的 `qmk-export/`（fetch-pro 注入） |
| 导出 UI | 弹窗/进度/结果 | `src/components/QmkExportOverlay.tsx` |
| 开关 | `isQmkExportEnabled()`（`plugins/index.ts:63`）控制 Web 版禁用 | `src/plugins/index.ts` |

> **stub 机制**：公开仓的 `plugins/qmk-export/` 只有 index/types 两个占位文件（函数 throw）。
> `npm run pro:fetch` 后会被私有仓真实源码整目录覆盖；`npm run pro:stub` 还原。
> ⛔ 占位文件禁加业务逻辑；改 `types.ts` 契约后用 `npm run pro:sync-stub` 回写私有仓同步。

### G. 插件系统（`src/plugins/index.ts`）

- `registerPlugin()` / `getEnabledPlugins()` / `initPluginSystem()`（`plugins/index.ts:30/42/70`）
- **free/pro 开关**：`isQmkExportEnabled()`（`plugins/index.ts:63`）——公开仓（未注入 Pro）时 Web 版 QMK 导出禁用
- 新增模块流程：`src/plugins/<module>/` 建目录 → `registerPlugin()` → `initPluginSystem()` 注册

### H. 主题与国际化

| 功能 | 定义 | 文件 |
|------|------|------|
| 主题系统 | 5 主题（classic/dark/material/future/business），CSS 变量 `--theme-*`，切换 html class | `src/lib/theme.tsx` + `src/app/globals.css` |
| ⛔ 新组件禁用硬编码色 | 一律用主题令牌（`--theme-primary/text/surface-hover` 等） | 全站纪律 |
| i18n | 9 语言（EN/简中/繁中/韩/日/俄/法/葡/西） | `src/lib/i18n.tsx`（**单文件 751 行，勿改名，破坏大量 import**） |
| 键帽纹理 | 6 种纹理 SVG（碳纤维/皮革/大理石/拉丝/哑光/木纹） | `src/data/textures.ts` |
| 色板 | KLE 标准色板 | `src/data/color-swatches.ts` |
| 预设 | 13 标准配列 + 9 复杂样本 | `src/data/presets.ts` |

### I. Tauri 桌面端（`src-tauri/`）

| 功能 | 定义 | 代码位置 |
|------|------|---------|
| STP 生成 | DXF→STP 3D 导出（cadrum/OpenCASCADE），多实体并列避免布尔卡死 | `src-tauri/src/lib.rs` `generate_stp` |
| 模型放置 | hotswap/Type-C/4P/MCU/RGB 模型装配（flip/z_offset 旋转） | `src-tauri/src/lib.rs` `assemble_models` |
| 3D 模型资产 | 编译时嵌入的生产级标准 STEP 文件（核心卖点） | `src-tauri/models/*.stp/.step`（7 个：hotswap/hotswap-pcb/Type-C/4P/MCU/RGB/T4） |
| 一键自动更新 | Tauri updater 插件查 GitHub Releases latest.json，minisign 签名校验 | `src/components/UpdateDialog.tsx` + `src-tauri/tauri.conf.json` plugins.updater |
| 平台桥接 | Tauri ↔ Web 双环境回退（`getPlatform()==="tauri"` 分支） | `src/lib/platform-bridge.ts` |

### J. AI 助手与工具层（v1.0.29 新增，`tools/` + `toolbelt/AiTab.tsx`）

| 功能 | 定义 | 文件 |
|------|------|------|
| 对话面板 | ToolBelt 第 11 Tab：OpenAI 兼容 API（DeepSeek/通义/OpenAI），Karpathy 风格系统提示词，工具调用可折叠，聊天记录 localStorage | `src/components/toolbelt/AiTab.tsx`（523 行） |
| 无头核心层 | 纯函数核心（预设/创建/编辑/导出/分享链接），供 CLI/MCP/AiTab 共用 | `tools/core.ts` |
| kdt CLI | `npm run kdt`：presets/new/edit/export/url 子命令 | `tools/cli.ts` |
| MCP server | stdio server，8 工具（get_guide/list_presets/create_layout/read_layout/edit_layout/export_layout/share_url/list_workspace） | `tools/mcp-server.ts` + 根目录 `.mcp.json` |
| AI 指南 | 给 LLM 读的 KLE 格式规范 + ops 语法 | `tools/AI_GUIDE.md` |

---

## 7. 测试体系

### 单测（`tests/`，Vitest，12 文件 443+ 用例）

| 文件 | 覆盖 |
|------|------|
| `kle-roundtrip.test.ts` / `kle-roundtrip-e2e.test.ts` | ⛔ 改 parser/serial 必跑（往返一致性） |
| `kle-urlon.test.ts` | URLON 编解码 |
| `svg-export.test.ts` | ⛔ 改 kle-export/key-renderer 必跑 |
| `plate-export.test.ts` / `pcb-export.test.ts` | ⛔ 改导出模块必跑（快照） |
| `coordinate-system.test.ts` | 坐标转换 |
| `pcb-matrix-overlay.test.ts` | PCB 矩阵叠加 |
| `fixture-validation.test.ts` / `security-and-interaction.test.ts` | 样本校验 / 安全与交互 |
| `tools-core.test.ts` | KDT 核心层（18 用例） |
| `mcp-smoke.test.ts` | MCP 兼容性（5 用例） |
| （Pro 测试） | ⚠ `qmk-export` / `kicad-export` 测试随 fetch-pro 注入 |

### E2E（`e2e/`，Playwright，3 spec）

`basic.spec.ts` / `toolbar.spec.ts` / `theme.spec.ts` — 使用 `data-testid` 定位。

---

## 8. 构建与发布

```bash
npm install                # postinstall 自动执行 fetch-pro --stub（零配置可构建）
npm run dev                # 开发（localhost:3000）
npm run build              # sync-version → next build → obfuscate（产物 out/）
npm run check              # lint + typecheck + build
npm run pro:fetch          # 从私有仓拉取真实 Pro 源码注入（PRO_REPO_TOKEN 或本机 git 凭证）
npm run pro:stub           # 用 pro-stub/ 还原公开仓状态（--force 强制覆盖）
npm run pro:sync-stub      # 把工作区 qmk-export/types.ts 契约回写 pro-stub/
npm run tauri:build:win    # Windows MSI（src-tauri/target/.../msi）
npm run kdt                # AI CLI（tools/cli.ts）
```

**Pro 注入机制**（`scripts/fetch-pro.mjs`）：PRO_MAP 定义私有仓路径 → 主仓目标
（`qmk-export/` → `src/plugins/qmk-export`、`kicad-export`/`lceda-export` → `src/lib`、
`keyboard-lceda-helper` → `plugins/`、`tests` → `tests/`）。所有 Pro 路径在 `.gitignore` 中。

**发布流程**：main 上 `npm run check` 通过 → bump 版本（`package.json` + `tauri.conf.json` +
`sync-version.mjs`）→ 打 `vX.Y.Z` 附注 tag → push 触发 **GitHub Actions**：
`ci.yml`（lint/typecheck/build/test）+ `release.yml`（mac/linux release；OCCT 预编译缓存自愈 +
cadrum 构建修复；**Windows 需本地构建** `npm run tauri:build:win`，产物手动传 GitHub Releases）。

**自动更新**：桌面版启动/顶栏「检查更新」→ updater 插件查
`https://github.com/709208969/keyboard-dev-toolkit/releases/latest/download/latest.json`
（`scripts/generate-latest-json.mjs` 生成）。发布前需在仓库 Secrets 配
`TAURI_SIGNING_PRIVATE_KEY` + `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`（密钥备份在
`K:\0AMAC\kle-editor\keys\`，勿丢）。安装包未签名（社区构建），Windows SmartScreen /
macOS 需"仍要运行/右键打开"。

**版本号纪律**：`package.json` 与 `src-tauri/tauri.conf.json` 必须同步
（`scripts/sync-version.mjs` 自动处理）；`<title>` 与页面版本徽标显示 APP_VERSION。

---

## 9. ⛔ 关键约束（不可违反，历史血泪教训）

1. **`_rawRows` 机制**：`kle-serial.ts` 的 `getRawRows()` / `exportJSON()` / `handleUpdate()` 三个函数**禁止修改**。任何数据操作必须保持 `_rawRows` 完整性；reducer 突变操作在选择集为空时必须 return state（已 guard）。
2. **SVG width/height**：`key-renderer.ts` 和 `kle-export.ts` 的 SVG 导出必须同时保留 `width`/`height` 原生属性 + `style="max-width:100%;height:auto"`，缺一不可（出过 bug）。
3. **坐标常量**：`KEY_UNIT_PX=54`（px）和 `U_MM=19.05`（mm）修改前必须先跑全部快照测试；坐标转换统一走 `coordinate-system.ts`，**禁止在其他文件定义坐标常量**。
4. **f2/fa 属性**：`kle-parser.ts` 中 `f2` 和 `fa` 必须用独立 `if` 而非 `else if`（可同时存在，else-if 会导致 fa 丢失）。
5. **NaN 序列化**：`kle-urlon.ts` 的 `stringify(NaN)` 必须输出 `:null`（JSON 标准）。
6. **i18n 文件位置**：`src/lib/i18n.tsx` 不要改名为 `i18n/index.tsx`（大量 import 路径依赖）。
7. **主题纪律**：新组件用 `--theme-*` CSS 变量，禁止硬编码色号（曾 37 处替换 0 处生效的教训）。
8. **Agent 防造假（历史教训）**：拆分/提取/新建模块任务后必须验证——`wc -l` 原文件行数确实减少 + `grep` 确认新文件被实际 import + 集成测试（含 QMK 模块 17 文件白建的教训）。
9. **构建产物纪律**：`npm run build` 会执行混淆脚本（obfuscate-build.mjs），调试时用 `npm run build:dev`。
10. **stub 纪律（公开仓特有）**：`src/lib/kicad-export.ts` / `lceda-export.ts` / `src/plugins/qmk-export/`
    为占位实现，**禁止添加业务逻辑**；签名必须与私有仓真实实现保持一致，否则公开仓类型检查漂移。
    `fetch-pro.mjs` 会以整目录/文件覆盖方式注入，stub 里写任何逻辑都会被丢弃。
11. **防泄漏纪律（公开仓特有）**：Pro 源码路径（`src/plugins/qmk-export/`、`/plugins/`）在
    `.gitignore` 中**永不提交**；clone 后未 fetch 时工作区只有 stub，勿把注入后的真实源码误提交进公开仓。

**修改前必跑测试对照表**：

| 要改什么 | 先跑什么 |
|---------|---------|
| `kle-parser.ts` / `kle-serial.ts` | `tests/kle-roundtrip*.test.ts` |
| `plate-export.ts` / `pcb-export.ts` | `tests/plate-export.test.ts` + `tests/pcb-export.test.ts` |
| `kle-export.ts` / `key-renderer.ts` | `tests/svg-export.test.ts` |
| `kle-urlon.ts` | `tests/kle-urlon.test.ts` |
| `coordinate-system.ts` / 坐标常量 | 全部测试 + 更新快照 |
| `matrix-core.ts` | `tests/pcb-matrix-overlay.test.ts` + `tools-core.test.ts` |
| `tools/` 核心层 | `tests/tools-core.test.ts` + `tests/mcp-smoke.test.ts` |
| `src/plugins/qmk-export/`（Pro 注入后） | 注入的 `tests/qmk-export.test.ts` |

---

## 10. 已知限制 / 待办（接手者可从这里入手）

- 拖拽吸附网格未实现（Options 步进 UI 已预留）
- Download SVG 为实验性功能
- 部分复杂样本（Kinesis Advantage）CSS 注入需微调
- 不支持 GitHub Gist 云端存储
- AI 面板需用户自备 OpenAI 兼容 API Key（DeepSeek/通义/OpenAI），仅存 localStorage；
  无内置后端代理，浏览器直接调 API（注意 CORS/密钥管理边界）
- 公开仓状态 QMK/KiCad/立创EDA 导出为 stub（调用即 throw）——需 `npm run pro:fetch`
  注入后才有完整功能；`QmkExportPanel.tsx` 已删除（旧 `useQmkExport.ts` 保留未用，可清理）
- 桌面安装包未签名（社区构建），发布需人工处理 SmartScreen/macOS 拦截提示
- Bug 跟踪与需求文档在**私有仓**（`项目经理.md` 等），公开仓仅存本路书 + README

---

## 11. 文档地图（已有文档导航）

| 文档 | 内容 | 价值 |
|------|------|------|
| `README.md` | 英文简介 + 功能概览 + 分步本地运行指南（文末中文简介） | 基础 |
| `项目简介.md` | 本项目必读速查（2KB） | 快速上手 |
| `项目路书.md` | 本文档 | 深度导航 |
| `tools/README.md` | KDT AI 工具层（CLI/MCP 用法 + 工具列表） | AI 开发 |
| `tools/AI_GUIDE.md` | 给 LLM 的 KLE 格式规范 + ops 语法 | AI 开发 |
| `docs/screenshots/` | README 功能截图 | 展示 |
| （私有仓） | 变更日志.md / 设计文档.md / CLAUDE.md / 项目经理.md / docs/*.md | 私有仓查阅 |

---

## 12. Git 仓库状态（2026-08-26）

- 当前分支：`main`（公开仓 19 commits，社区版精简历史；旧私有仓历史已重写，**勿引用旧 hash**）
- 远程：`origin` = GitHub `git@github_kevinxu:709208969/keyboard-dev-toolkit.git`（无 Gitee 远程）
- Pro 私有仓：`gitee.com/kevinxu93/keyboard-editor-pro`（fetch-pro 凭据：`PRO_REPO_TOKEN` 或本机 git 凭据管理器）
- 提交规范：Conventional Commits（feat/fix/refactor/docs/test/chore/perf/ci）
- 发布：main 打 `vX.Y.Z` tag → GitHub Actions `release.yml`（mac/linux CI + Windows 本地构建）
- 测试基建：`npm run check:full`（单测 + lint + typecheck + build）全绿再发版

---

> 本路书维护约定：每次大版本（v1.0.x → v1.1.0 或架构重构）后更新；新增功能模块时在 §6 对应分组补充一行。
