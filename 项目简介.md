# 项目简介 — Keyboard Dev Toolkit（公开仓·社区版）

> **本文件 = 项目驾驶舱。每天开新会话先读这里**：看「当前状态」→ 认领「下一步任务」最上面一条 → 按「文档索引」读对应细节 → 执行 → 回来更新本文件。
> 保密纪律：商业 / 内部细节只放在 `docs/` 下已 gitignore 的内部文档；本文件只放状态与指针，不放成本 / 倍率 / 店铺敏感信息。

## 当前状态（2026-09-16）

| 工作流 | 状态 | 说明 |
|---|---|---|
| 战略（开放管道） | ✅ D1 已接受（09-16） | 编辑器免费开源 + 订单 API 对外开放；详见 `docs/商业实践计划.md` |
| 一键下单（阶段 0） | 🟡 后端已生产验收（09-10），**前端下单 UI 未接** | checkout 五道闸门 + 快照 + App Proxy 验签 + 建单已上线；链路探活 `{"ok":true}` |
| 4PX 国际运费 | ⚪ 未接真（当前 mock） | 方案 Phase 2 |
| 白手套内测（D4） | ⚪ SOP 已定稿，待启动 | `docs/商业实践计划.md` §4.1 |
| 订单中枢（内部运营系统） | ⚪ 计划已出，未开工（先 O0 台账） | `docs/订单中枢系统计划.md` |
| 桌面版发布 | 🟢 v1.0.56 三平台（Win/mac/Linux） | GitHub Releases + 自动更新 |

> ⚠️ **风险提示**：`K:\0AMAC\kindlestar-pricing` 有大量**未提交**代码（`checkout/`、`docs/`、`scripts/`、`tests/`、`wrangler.toml` 改动）——先提交入库防丢失（提交前跑 `scan-secrets`）。

## 下一步任务（按优先级，完成打勾）

- [ ] **P0** 提交 kindlestar-pricing 未提交代码（checkout 等，防丢失；提交前 `node scripts/scan-secrets.mjs`）
- [ ] **P0** 前端下单 UI：照 `docs/一键下单方案.md` §8（quote-api 扩展 `estimateShipping` / `createCheckout` + PricingSection 下单流程 + i18n + 测试）
- [ ] **P1** 安全加固拍板：建单接口开放缓解（Turnstile / 限流 / 草稿单清理，见 kindlestar-pricing `docs/AppProxy配置指引.md` §7.3）
- [ ] **P1** 4PX 真接口（方案 Phase 2：签名 + 试算 + 泡货过滤 + 白名单）
- [ ] **P1** 白手套内测启动：招募 5-10 人（SOP 见 `docs/商业实践计划.md` §4.1）
- [ ] **P2** 订单中枢 O0：台账先行 + 摸清流程节点（见 `docs/订单中枢系统计划.md`）
- [ ] **P2** 漏斗埋点（`docs/软件落地计划.md` T0.2）

## 文档索引

| 文档 | 位置 | 用途 |
|---|---|---|
| 商业实践计划 | `docs/商业实践计划.md`（内部） | 战略 / 收入 / PR / 营销 / 白手套 SOP / D1-D4 |
| 软件落地计划 | `docs/软件落地计划.md`（内部） | 工程路线图（阶段 0-3） |
| 订单中枢系统计划 | `docs/订单中枢系统计划.md`（内部） | 内部订单 / 项目管理系统 + AI 护栏框架 |
| 一键下单方案 | `docs/一键下单方案.md`（内部·冻结） | 阶段 0 执行手册（后端已按此实现，前端 §8 待做） |
| 生产域名与网络方案 | `K:\0AMAC\kindlestar-pricing\docs\` | App Proxy 方案（已实施·含验收证据） |
| 项目路书 | `项目路书.md` | 技术细节 / 精确行号 / 发布流程 |
| 调试手册 / 计价方式 | 根目录（gitignored） | 调试 / 计价细节 |

## 每日推进规则

1. 新会话第一句：「读 项目简介.md，认领下一步任务」
2. 执行前读对应文档；执行中一次一命令（dcg）、改代码跑 `npm run check`
3. 执行后更新本文件（勾选 / 状态 / 新问题），重要结论写回对应内部文档
4. 纪律：保密红线（成本 / 倍率不进对外产物）、双仓纪律（Pro 走 fetch-pro）、版本号纪律（每次更新 bump）

---

## 技术速查（AI 调试必读）

### 是什么

键盘配列编辑器（[KLE](https://www.keyboard-layout-editor.com) 数据格式兼容）+ 键盘 PCB/定位板自动生成工具 + **AI 设计助手**。
单页应用：`src/app/page.tsx` → `EditorPage.tsx`（406 行主编排器）。
本仓库为 **AGPL-3.0 社区版（公开仓）**，Pro 模块（QMK/KiCad/立创EDA）由 `fetch-pro.mjs` 从私有仓注入。

### 技术栈

Next.js 16 / React 19 / TS 5 / Tailwind v4 / Tauri v2 (Rust) / Vitest + Playwright。
全客户端渲染，静态导出 `out/`。AI 工具层基于 MCP SDK + zod。

### 常用命令

| 命令 | 用途 |
|------|------|
| `npm run dev` | 开发（localhost:3000） |
| `npm run check` | lint + typecheck + build |
| `npx vitest run tests/` | 单测（14 文件 462 用例） |
| `npm run test:e2e` | Playwright E2E |
| `npm run kdt` | KDT AI CLI（`tools/cli.ts`） |
| `npm run pro:fetch` | 从私有仓注入 Pro 源码（需 Gitee 凭证） |
| `npm run pro:stub` | 还原公开仓 stub 占位（postinstall 自动执行） |

### 核心数据流

```
URL hash(##@@)/localStorage/JSON
  → parseKLE / parseKLEJSON (lib/kle-parser.ts / kle-serial.ts)
  → KeyProps[] (内部格式, 12 标签位)
  → editorReducer (lib/kle-reducer.ts, useReducer + Undo/Redo)
  → KeyboardCanvas / ToolBelt(11Tab) / PlateSection / PCBSection / AiTab
  → 导出 JSON/SVG/PNG/JPG/DXF/STP(桌面) + Pro注入后: KiCad/立创EDA/QMK
外部: tools/core.ts ← kdt CLI / MCP server（AI 工具层）
```

### 关键文件速查

| 模块 | 位置 |
|------|------|
| 状态入口 | `hooks/useKeyboardEditor.ts` |
| 类型定义 | `lib/kle-types.ts`（KeyProps:97 / KLEMeta:188 / KLELayout:265） |
| 画布 | `components/KeyboardCanvas.tsx` + `canvas/` |
| 属性面板 | `components/ToolBelt.tsx` + `toolbelt/`（11 Tab，含 AiTab 523 行） |
| AI 助手 | `toolbelt/AiTab.tsx` + `tools/`（core.ts / cli.ts / mcp-server.ts） |
| 矩阵引擎 | `lib/matrix-core.ts` `assignMatrix:34`（PCB 预览/导出共用） |
| PCB/Plate | `lib/pcb-export.ts` `generatePCB:364` / `lib/plate-export.ts` `generatePlate:331` |
| **PCBA 计价** | 服务端 Worker（`kindlestar-pricing` 仓库，当前 v2.9.0）+ `lib/quote-api.ts`（API 封装，走 App Proxy）+ `components/PricingSection.tsx` + `components/PlatePricingSection.tsx`——**安装版依赖 `tauri.conf.json` CSP 白名单（见 ⛔10）**，细节见 `计价方式.md` |
| QMK 导出 | `plugins/qmk-export/`（**Pro stub**，fetch-pro 注入后可用）；社区版已移除 footer 入口按钮（2026-09-17），弹窗组件 `QmkExportOverlay` 保留 |
| 坐标转换 | `lib/coordinate-system.ts`（唯一入口，KEY_UNIT_PX=54 / U_MM=19.05） |
| 主题 | `lib/theme.tsx` + `app/globals.css`（5 主题 `--theme-*` 变量） |
| i18n | `lib/i18n.tsx`（单文件 751 行 9 语言，勿改名） |
| Rust 后端 | `src-tauri/src/lib.rs`（STP 生成 + 7 个 STEP 模型装配） |
| 自动更新 | `components/UpdateDialog.tsx` + Tauri updater（GitHub Releases）；安装版走 `latest.json`（NSIS），**绿色版 portable exe** 走 `latest-portable.json`（下载新 exe 原地替换，Rust `lib.rs` portable 命令） |

### ⛔ 不可违反（详见路书 §9）

1. `_rawRows` 机制（`kle-serial.ts` 的 getRawRows/exportJSON）**禁改**
2. SVG 导出必须同时保留 width/height + `max-width:100%`
3. 坐标常量 `KEY_UNIT_PX=54` / `U_MM=19.05` 改前跑全部快照测试
4. `f2`/`fa` 用独立 if（勿改回 else-if）
5. URLON `stringify(NaN)` 输出 `:null`
6. 新组件用 `--theme-*` 变量，禁硬编码色
7. 改 parser/serial → 跑 `tests/kle-roundtrip*.test.ts`；改导出 → 跑对应快照测试
8. **stub 纪律**：`lib/kicad-export.ts` / `lib/lceda-export.ts` / `plugins/qmk-export/`
   禁加业务逻辑，签名必须与私有仓一致（fetch-pro 会覆盖）
9. **防泄漏**：Pro 路径已入 `.gitignore`（`src/plugins/qmk-export/`、`/plugins/`），
   禁止把真实 Pro 源码提交进公开仓
10. **CSP 同步**：改报价/汇率/AI 的 API 域名必须同步 `src-tauri/tauri.conf.json` 的
    `connect-src`（安装版 webview 强制；dev 浏览器不注入、测不出），守卫测试 `tests/csp-connect-src.test.ts`

### 什么时候该读 `项目路书.md`

改坐标/渲染/导出逻辑、新增功能模块、涉及 Pro 注入/发布、或需要精确函数行号时。

### 备注

- **双仓架构**：公开仓 = 社区版（AGPL-3.0，GitHub `kevinx-key/keyboard-dev-toolkit`）；
  私有仓 = Pro 源码（Gitee `kevinxu93/keyboard-editor-pro`），`scripts/fetch-pro.mjs`
  三模式（fetch / --stub / --sync-stub）负责注入与契约同步
- 桌面版已发布 win/mac/linux（当前 v1.0.56，GitHub Releases）；
  Windows 已接入 SignPath Foundation 免费签名流程（配置密钥后自动生效，见 `docs/signpath-签名指南.md`）；
  macOS 未签名需「仍要运行」，首推一键脚本 `scripts/install-macos.sh`（自动装 + 解除 quarantine）；
  macOS 15+ 需系统设置 → 隐私与安全性 →「仍然打开」；
  自动更新经 updater 插件查 latest.json，签名密钥在仓库 Secrets
  （`TAURI_SIGNING_PRIVATE_KEY` + PASSWORD，备份在 `K:\0AMAC\kle-editor\keys\`，勿丢）
- AI 助手需用户自备 OpenAI 兼容 API Key（DeepSeek/通义/OpenAI），仅存 localStorage
- 已知限制：网格吸附未实现、Gist 云存储不支持
- 发布：main 打 `vX.Y.Z` tag 触发 GitHub Actions release.yml（mac/linux/win 三平台 CI 构建；
  Windows 本地构建可选，需 updater 签名密钥，见路书 §8）；产物 = NSIS 安装包 + **绿色版 portable exe**
  + macOS dmg/`.app.tar.gz` + Linux AppImage（均带 `.sig`，供自动更新）
- C5/S3 小板已从全部仓库（公开/私有/Gitee）历史中清除，git 操作勿引用旧 hash
